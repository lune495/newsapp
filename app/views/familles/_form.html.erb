<%= form_with(model: famille, local: true) do |form| %>
  <% if famille.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(famille.errors.count, "error") %> prohibited this famille from being saved:</h2>

      <ul>
        <% famille.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
